import CodeEditor from './index.vue';
import { LANGUAGE_OPTIONS, THEME_OPTIONS } from './constants';
import type { CodeEditorProps, CodeEditorEmits } from './types';

export { LANGUAGE_OPTIONS, THEME_OPTIONS };
export type { CodeEditorProps, CodeEditorEmits };
export default CodeEditor;
